#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAXSTRING 101

struct tuple {
    unsigned char string[MAXSTRING];
    unsigned long timestamp;
};
typedef struct tuple tuple;

int main() {

}